/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ["@web/ui"],
  // output: "export",
  // trailingSlash: true,
  // images: {
  //   unoptimized: true,
  // },
};

export default nextConfig;
